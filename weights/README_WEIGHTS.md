# GSN Weights

Place your GSN model checkpoint (`.pt` file) in this directory.
The application will automatically look for files with "phase", "checkpoint", "gsn", "unet", "final", or "best" in their name.

If no weights are found, you will be prompted to enter the path to a checkpoint in the sidebar.
